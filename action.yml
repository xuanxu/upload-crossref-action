name: Crossref Uploader
description: Uploads the paper JATS file
inputs:
  jats_path:
    description: The path to the JATS file
    required: true
  papers_repo:
    description: The repository containing the published and submitted papers
    required: true
  issue_id:
    description: The issue number of the submission
    required: true
  branch_prefix:
    description: The prefix to add to the name of all branches
    required: false
    default: ""
  bot_token:
    description: The access token to be used to upload files
    required: true
outputs:
  html_url:
    description: "HTML URL for the uploaded JATS file"
    value: ${{ steps.jats_uploader.outputs.jats_html_url }}
  download_url:
    description: "Download URL for the uploaded JATS file"
    value: ${{ steps.jats_uploader.outputs.jats_download_url }}
runs:
  using: "composite"
  steps:
    - name: Install Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 3.0.3
        bundler-cache: true
        cache-version: upload-jats
        bundler: Gemfile.lock
        working-directory: ${{ github.action_path }}
    - name: Upload JATS file to paper branch
      id: jats_uploader
      shell: bash
      env:
        PAPERS_REPO: ${{ inputs.papers_repo }}
        BRANCH_PREFIX: ${{ inputs.branch_prefix }}
        ISSUE_ID: ${{ inputs.issue_id }}
        JATS_PATH: ${{ inputs.jats_path }}
        BOT_TOKEN: ${{ inputs.bot_token }}
        BUNDLE_GEMFILE: ${{ github.action_path }}/Gemfile
      run: |
        bundle exec ruby ${{ github.action_path }}/add_jats_to_branch.rb
    - name: Print results
      shell: bash
      run: |
        echo "JATS file HTML URL: ${{ steps.jats_uploader.outputs.jats_html_url }}"
        echo "JATS file download URL: ${{ steps.jats_uploader.outputs.jats_download_url }}"

branding:
  icon: upload
  color: green
